# backend

## Execute

* Run `stack exec -- backend-exe` to see "We're inside the application!"
* With `stack exec -- backend-exe --verbose` you will see the same message, with more logging.

## Run tests

`stack test`
